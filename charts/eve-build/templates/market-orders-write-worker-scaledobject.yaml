{{- if and .Values.rabbitmq.enabled .Values.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ .Release.Name }}-market-orders-write-worker
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  scaleTargetRef:
    name: {{ .Release.Name }}-market-orders-write-worker
  minReplicaCount: {{ .Values.eveBuild.marketOrders.writeWorker.keda.minReplicas }}
  maxReplicaCount: {{ .Values.eveBuild.marketOrders.writeWorker.keda.maxReplicas }}
  pollingInterval: {{ .Values.eveBuild.marketOrders.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.eveBuild.marketOrders.keda.cooldownPeriod }}
  triggers:
    - type: rabbitmq
      metadata:
        queueName: market_orders.results
        mode: QueueLength
        value: {{ .Values.eveBuild.marketOrders.writeWorker.keda.queueLength | quote }}
        protocol: amqp
      authenticationRef:
        name: {{ .Release.Name }}-rabbitmq-conn
{{- end }}

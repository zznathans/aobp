{{- if .Values.eveBuild.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
  {{- with .Values.eveBuild.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.eveBuild.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  rules:
    - host: {{ required "eveBuild.ingress.host is required when eveBuild.ingress.enabled" .Values.eveBuild.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}
                port:
                  number: {{ .Values.eveBuild.service.port }}
  {{- if .Values.eveBuild.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.eveBuild.ingress.host | quote }}
      secretName: {{ required "eveBuild.ingress.tls.secretName is required when eveBuild.ingress.tls.enabled" .Values.eveBuild.ingress.tls.secretName }}
  {{- end }}
{{- end }}

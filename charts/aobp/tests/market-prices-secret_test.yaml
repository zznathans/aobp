suite: market-prices-secret
templates:
  - market-prices-secret.yaml
tests:
  - it: fails when refreshApiKey is unset and no existingSecret is given
    asserts:
      - failedTemplate:
          errorMessage: aobp.marketPrices.refreshApiKey is required unless aobp.marketPrices.existingSecret is set - generate one with `openssl rand -hex 32`

  - it: renders a Secret with the given refreshApiKey
    set:
      aobp.marketPrices.refreshApiKey: my-api-key
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Secret
      - equal:
          path: metadata.name
          value: RELEASE-NAME-market-prices
      - equal:
          path: stringData.apiKey
          value: my-api-key

  - it: renders nothing when existingSecret is set, even without refreshApiKey
    set:
      aobp.marketPrices.existingSecret: my-market-prices-secret
    asserts:
      - hasDocuments:
          count: 0

# Normalize everything to LF in the repo, regardless of the checkout
# platform's core.autocrlf setting - this repo has no Windows-specific
# tooling that needs CRLF, and mixed line endings were causing every git
# operation on text files to print "will be replaced by CRLF" warnings.
* text=auto eol=lf
